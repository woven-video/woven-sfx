# Woven SFX Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship v0 of `sfx.woven.video` — FOSS agent-first SFX registry with landing page (Astro), MCP tools, skill, install script, and ~27 sounds on Cloudflare.

**Architecture:** pnpm monorepo — `packages/core` (catalog + resolve + pull), `packages/mcp` (stdio MCP), `packages/skill` (SKILL.md), `apps/web` (Astro static site). Audio + peaks on R2/CDN; site on Cloudflare Workers static assets via Wrangler.

**Tech Stack:** pnpm, TypeScript, Vitest, `@modelcontextprotocol/server`, Astro 5, React 19 islands, Tailwind 4, `@wavesurfer/react`, Wrangler, R2

**Docs digest:** `docs/superpowers/research/2026-06-28-woven-sfx-docs.md`

**Spec:** `docs/superpowers/specs/2026-06-28-woven-sfx-design.md`

---

## File map

| Path | Responsibility |
|---|---|
| `package.json` | Root workspace scripts |
| `pnpm-workspace.yaml` | Monorepo packages |
| `packages/core/src/paths.ts` | `SFX_HOME`, `SFX_LIBRARY` constants |
| `packages/core/src/types.ts` | `Sound`, `Catalog` types |
| `packages/core/src/pairings.ts` | Transition → sound ID rules |
| `packages/core/src/catalog.ts` | Load/search catalog |
| `packages/core/src/resolve.ts` | `resolveForTransition()` |
| `packages/core/src/pull.ts` | Download wav to library |
| `packages/core/src/installed.ts` | List cached files |
| `packages/mcp/src/index.ts` | MCP server + 4 tools |
| `packages/skill/SKILL.md` | Agent workflow |
| `scripts/install.sh` | One-click install |
| `scripts/build-catalog.ts` | Merge `catalog/sounds/*.json` → `catalog.json` |
| `scripts/generate-peaks.ts` | ffmpeg/audiowaveform → peaks JSON |
| `catalog/sounds/*.json` | Per-sound metadata |
| `sounds/*.wav` | Source audio (git-lfs or local only) |
| `apps/web/` | Astro landing page |
| `apps/web/src/components/*` | UI components per spec |
| `apps/web/public/catalog.json` | Built catalog for site |
| `apps/web/public/llms.txt` | Agent-readable index |
| `apps/web/wrangler.jsonc` | Cloudflare deploy config |

---

## Prerequisites (human)

Before Task 5, place `.wav` files in `sounds/` (copy from your Remotion `shared-assets/sfx/` if available). Filenames: `{id}.wav` lowercase.

---

### Task 1: Monorepo scaffold

**Files:**
- Create: `package.json`, `pnpm-workspace.yaml`, `LICENSE`, `SOUNDS_LICENSE`, `README.md`, `tsconfig.base.json`

- [ ] **Step 1: Create workspace root**

`package.json`:
```json
{
  "name": "woven-sfx",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "build:catalog": "tsx scripts/build-catalog.ts",
    "generate:peaks": "tsx scripts/generate-peaks.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  },
  "packageManager": "pnpm@9.15.0"
}
```

`pnpm-workspace.yaml`:
```yaml
packages:
  - "packages/*"
  - "apps/*"
```

`tsconfig.base.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  }
}
```

- [ ] **Step 2: Add licenses and README**

`LICENSE` — MIT full text.

`SOUNDS_LICENSE` — CC0 1.0 statement.

`README.md` — project one-liner, link to sfx.woven.video, install command, monorepo layout.

- [ ] **Step 3: Install and verify**

```bash
cd /Users/naman/projects/woven-sfx
pnpm install
```

Expected: lockfile created, no errors.

- [ ] **Step 4: Commit**

```bash
git add package.json pnpm-workspace.yaml tsconfig.base.json LICENSE SOUNDS_LICENSE README.md
git commit -m "chore: scaffold pnpm monorepo"
```

---

### Task 2: packages/core — paths & types

**Files:**
- Create: `packages/core/package.json`, `packages/core/tsconfig.json`, `packages/core/vitest.config.ts`
- Create: `packages/core/src/paths.ts`, `packages/core/src/types.ts`
- Create: `packages/core/src/paths.test.ts`

- [ ] **Step 1: Package setup**

`packages/core/package.json`:
```json
{
  "name": "@woven-sfx/core",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
```

`packages/core/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "outDir": "./dist", "rootDir": "./src" },
  "include": ["src/**/*"]
}
```

- [ ] **Step 2: Write failing test**

`packages/core/src/paths.test.ts`:
```typescript
import { describe, it, expect } from "vitest";
import { getSfxHome, getSfxLibrary } from "./paths.js";

describe("paths", () => {
  it("returns woven-sfx under XDG data home", () => {
    expect(getSfxHome()).toMatch(/woven-sfx$/);
    expect(getSfxLibrary()).toMatch(/library$/);
  });
});
```

- [ ] **Step 3: Run test — expect FAIL**

```bash
cd packages/core && pnpm test
```
Expected: FAIL — cannot find module `./paths.js`

- [ ] **Step 4: Implement paths + types**

`packages/core/src/paths.ts`:
```typescript
import { homedir } from "node:os";
import { join } from "node:path";

export function getSfxHome(): string {
  const base = process.env.XDG_DATA_HOME ?? join(homedir(), ".local", "share");
  return join(base, "woven-sfx");
}

export function getSfxLibrary(): string {
  return join(getSfxHome(), "library");
}

export const DEFAULT_CDN_BASE = "https://sfx.woven.video";
```

`packages/core/src/types.ts`:
```typescript
export type Sound = {
  id: string;
  file: string;
  tags: string[];
  duration_ms: number;
  pairings?: { transition?: string[]; moment?: string[] };
  default_volume: number;
  url: string;
  peaks_url?: string;
};

export type Catalog = {
  version: string;
  sounds: Sound[];
};

export type ResolvedSound = {
  id: string;
  file: string;
  localPath: string;
  duration_ms: number;
  suggested_volume: number;
  url: string;
};
```

`packages/core/src/index.ts`:
```typescript
export * from "./paths.js";
export * from "./types.js";
```

- [ ] **Step 5: Run test — expect PASS**

```bash
cd packages/core && pnpm test && pnpm build
```

- [ ] **Step 6: Commit**

```bash
git add packages/core
git commit -m "feat(core): add paths and catalog types"
```

---

### Task 3: packages/core — pairings & resolve

**Files:**
- Create: `packages/core/src/pairings.ts`, `packages/core/src/resolve.ts`
- Create: `packages/core/src/resolve.test.ts`

- [ ] **Step 1: Write failing test**

`packages/core/src/resolve.test.ts`:
```typescript
import { describe, it, expect } from "vitest";
import { resolveForTransition } from "./resolve.js";
import type { Catalog } from "./types.js";

const catalog: Catalog = {
  version: "1.0.0",
  sounds: [
    {
      id: "swish-whoosh-large",
      file: "swish-whoosh-large.wav",
      tags: ["whoosh"],
      duration_ms: 680,
      default_volume: 0.45,
      url: "https://sfx.woven.video/sfx/swish-whoosh-large.wav",
      pairings: { transition: ["pull-in"] },
    },
    {
      id: "fast-whoosh",
      file: "fast-whoosh.wav",
      tags: ["whoosh"],
      duration_ms: 420,
      default_volume: 0.45,
      url: "https://sfx.woven.video/sfx/fast-whoosh.wav",
      pairings: { transition: ["pull-out", "whoosh"] },
    },
  ],
};

describe("resolveForTransition", () => {
  it("returns swish-whoosh-large for pull-in", () => {
    const r = resolveForTransition(catalog, "pull-in");
    expect(r?.id).toBe("swish-whoosh-large");
    expect(r?.suggested_volume).toBe(0.45);
  });

  it("returns fast-whoosh for pull-out", () => {
    const r = resolveForTransition(catalog, "pull-out");
    expect(r?.id).toBe("fast-whoosh");
  });

  it("returns null for fade", () => {
    expect(resolveForTransition(catalog, "fade")).toBeNull();
  });
});
```

- [ ] **Step 2: Run test — expect FAIL**

```bash
cd packages/core && pnpm test
```

- [ ] **Step 3: Implement pairings + resolve**

`packages/core/src/pairings.ts`:
```typescript
/** Primary sound ID per transition (from edit-plan skill). */
export const TRANSITION_PRIMARY: Record<string, string> = {
  "pull-in": "swish-whoosh-large",
  "pull-out": "fast-whoosh",
  flash: "camera-flash",
  glitch: "glitch-logo",
  "tv-glitch": "glitch-logo",
  whoosh: "fast-whoosh",
};

/** fade → no SFX by default */
export const TRANSITION_NONE = new Set(["fade", "none"]);
```

`packages/core/src/resolve.ts`:
```typescript
import { join } from "node:path";
import { TRANSITION_NONE, TRANSITION_PRIMARY } from "./pairings.js";
import type { Catalog, ResolvedSound } from "./types.js";
import { getSfxLibrary } from "./paths.js";

export function resolveForTransition(
  catalog: Catalog,
  transition: string,
): Omit<ResolvedSound, "localPath"> | null {
  const key = transition.toLowerCase();
  if (TRANSITION_NONE.has(key)) return null;

  const primaryId = TRANSITION_PRIMARY[key];
  if (!primaryId) return null;

  const sound = catalog.sounds.find((s) => s.id === primaryId);
  if (!sound) return null;

  return {
    id: sound.id,
    file: sound.file,
    duration_ms: sound.duration_ms,
    suggested_volume: sound.default_volume,
    url: sound.url,
  };
}

export function withLocalPath(
  resolved: Omit<ResolvedSound, "localPath">,
): ResolvedSound {
  return {
    ...resolved,
    localPath: join(getSfxLibrary(), resolved.file),
  };
}
```

Update `packages/core/src/index.ts` to export pairings + resolve.

- [ ] **Step 4: Run test — expect PASS**

```bash
cd packages/core && pnpm test
```

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/pairings.ts packages/core/src/resolve.ts packages/core/src/resolve.test.ts
git commit -m "feat(core): add transition resolve logic"
```

---

### Task 4: packages/core — catalog search, pull, installed

**Files:**
- Create: `packages/core/src/catalog.ts`, `packages/core/src/pull.ts`, `packages/core/src/installed.ts`
- Create: `packages/core/src/catalog.test.ts`

- [ ] **Step 1: Write failing catalog search test**

`packages/core/src/catalog.test.ts`:
```typescript
import { describe, it, expect } from "vitest";
import { searchCatalog } from "./catalog.js";
import type { Catalog } from "./types.js";

const catalog: Catalog = {
  version: "1.0.0",
  sounds: [
    { id: "fast-whoosh", file: "fast-whoosh.wav", tags: ["whoosh"], duration_ms: 420, default_volume: 0.45, url: "" },
    { id: "glitch-logo", file: "glitch-logo.wav", tags: ["glitch"], duration_ms: 320, default_volume: 0.4, url: "" },
  ],
};

describe("searchCatalog", () => {
  it("filters by tag", () => {
    expect(searchCatalog(catalog, { tag: "glitch" }).map((s) => s.id)).toEqual(["glitch-logo"]);
  });
  it("filters by query", () => {
    expect(searchCatalog(catalog, { query: "whoosh" }).map((s) => s.id)).toEqual(["fast-whoosh"]);
  });
});
```

- [ ] **Step 2: Implement catalog.ts**

```typescript
import type { Catalog, Sound } from "./types.js";

export function searchCatalog(
  catalog: Catalog,
  opts: { tag?: string; transition?: string; query?: string },
): Sound[] {
  let results = catalog.sounds;
  if (opts.tag) {
    const t = opts.tag.toLowerCase();
    results = results.filter((s) => s.tags.some((tag) => tag.toLowerCase() === t));
  }
  if (opts.transition) {
    const tr = opts.transition.toLowerCase();
    results = results.filter((s) =>
      s.pairings?.transition?.some((p) => p.toLowerCase() === tr),
    );
  }
  if (opts.query) {
    const q = opts.query.toLowerCase();
    results = results.filter(
      (s) => s.id.includes(q) || s.tags.some((t) => t.includes(q)),
    );
  }
  return results;
}
```

- [ ] **Step 3: Implement pull.ts + installed.ts**

`packages/core/src/pull.ts`:
```typescript
import { mkdir, writeFile, access } from "node:fs/promises";
import { join } from "node:path";
import { getSfxLibrary } from "./paths.js";
import type { Catalog, Sound } from "./types.js";

export async function fileExists(path: string): Promise<boolean> {
  try {
    await access(path);
    return true;
  } catch {
    return false;
  }
}

export async function pullSound(sound: Sound): Promise<string> {
  const dir = getSfxLibrary();
  await mkdir(dir, { recursive: true });
  const dest = join(dir, sound.file);
  if (await fileExists(dest)) return dest;

  const res = await fetch(sound.url);
  if (!res.ok) throw new Error(`Failed to download ${sound.url}: ${res.status}`);
  const buf = Buffer.from(await res.arrayBuffer());
  await writeFile(dest, buf);
  return dest;
}

export function findSoundById(catalog: Catalog, id: string): Sound | undefined {
  return catalog.sounds.find((s) => s.id === id);
}
```

`packages/core/src/installed.ts`:
```typescript
import { readdir } from "node:fs/promises";
import { getSfxLibrary } from "./paths.js";

export async function listInstalled(): Promise<string[]> {
  try {
    const files = await readdir(getSfxLibrary());
    return files.filter((f) => f.endsWith(".wav")).sort();
  } catch {
    return [];
  }
}
```

- [ ] **Step 4: Run tests + build**

```bash
cd packages/core && pnpm test && pnpm build
```

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/catalog.ts packages/core/src/pull.ts packages/core/src/installed.ts packages/core/src/catalog.test.ts packages/core/src/index.ts
git commit -m "feat(core): add catalog search, pull, and installed list"
```

---

### Task 5: Catalog seed + build script

**Files:**
- Create: `scripts/build-catalog.ts`
- Create: `catalog/sounds/*.json` (27 files)
- Create: `apps/web/public/catalog.json` (generated)

**Sound IDs to seed:**
`airplane-ding`, `base-drop`, `bass-impact`, `beep-short`, `camera-analog`, `camera-flash`, `camera-shutter-release`, `deep-whoosh`, `dj-scratch`, `elevator-ding`, `fast-whoosh`, `frog-ribbit`, `glitch-logo`, `keyboard-typing`, `keyboard-typing-realistic`, `message-pop-reply`, `money-chalking`, `mouse-click-1`, `mouse-click-2`, `notification-ding`, `pop-hand`, `record-needle-scratch`, `record-scratch`, `several-coins`, `sideways-record-needle`, `swish-whoosh-large`, `tick-sound`

- [ ] **Step 1: Create build-catalog script**

`scripts/build-catalog.ts`:
```typescript
import { readdir, readFile, writeFile, mkdir } from "node:fs/promises";
import { join } from "node:path";

const CDN = process.env.SFX_CDN_BASE ?? "https://sfx.woven.video";
const SOUNDS_DIR = join(import.meta.dirname, "../catalog/sounds");
const OUT_PATHS = [
  join(import.meta.dirname, "../apps/web/public/catalog.json"),
  join(import.meta.dirname, "../packages/mcp/catalog.json"),
];

async function main() {
  const files = (await readdir(SOUNDS_DIR)).filter((f) => f.endsWith(".json"));
  const sounds = await Promise.all(
    files.map(async (f) => {
      const raw = await readFile(join(SOUNDS_DIR, f), "utf-8");
      const sound = JSON.parse(raw);
      return {
        ...sound,
        file: sound.file ?? `${sound.id}.wav`,
        url: sound.url ?? `${CDN}/sfx/${sound.id}.wav`,
        peaks_url: sound.peaks_url ?? `${CDN}/peaks/${sound.id}.json`,
      };
    }),
  );
  sounds.sort((a, b) => a.id.localeCompare(b.id));
  const catalog = { version: "1.0.0", sounds };
  for (const out of OUT_PATHS) {
    await mkdir(join(out, ".."), { recursive: true });
    await writeFile(out, JSON.stringify(catalog, null, 2));
  }
  console.error(`Built catalog with ${sounds.length} sounds`);
}

main();
```

- [ ] **Step 2: Create per-sound JSON template**

Example `catalog/sounds/fast-whoosh.json`:
```json
{
  "id": "fast-whoosh",
  "tags": ["whoosh", "transition"],
  "duration_ms": 420,
  "default_volume": 0.45,
  "pairings": { "transition": ["pull-out", "whoosh"] }
}
```

Create all 27 files. Tag hints:
- whoosh: `fast-whoosh`, `deep-whoosh`, `swish-whoosh-large`
- glitch: `glitch-logo`, `record-scratch`, `dj-scratch`
- pop: `pop-hand`, `camera-flash`, `message-pop-reply`
- ding: `airplane-ding`, `elevator-ding`, `notification-ding`
- other: assign `ui`, `ambient`, `impact` as appropriate

Use `ffprobe` to fill accurate `duration_ms` once wavs exist:
```bash
ffprobe -v quiet -show_entries format=duration -of csv=p=0 sounds/fast-whoosh.wav
```

- [ ] **Step 3: Run build**

```bash
pnpm build:catalog
```

Expected: `apps/web/public/catalog.json` and `packages/mcp/catalog.json` created.

- [ ] **Step 4: Commit**

```bash
git add scripts/build-catalog.ts catalog/sounds apps/web/public/catalog.json packages/mcp/catalog.json
git commit -m "feat: seed catalog with 27 sounds and build script"
```

---

### Task 6: Peak generation script

**Files:**
- Create: `scripts/generate-peaks.ts`
- Create: `apps/web/public/peaks/*.json`

- [ ] **Step 1: Implement peak generator**

`scripts/generate-peaks.ts` — for each `sounds/*.wav`, use wavesurfer's audiowaveform or a simple downsample:

```typescript
import { readdir, writeFile, mkdir } from "node:fs/promises";
import { join } from "node:path";
import { execSync } from "node:child_process";

const SOUNDS = join(import.meta.dirname, "../sounds");
const OUT = join(import.meta.dirname, "../apps/web/public/peaks");

async function main() {
  await mkdir(OUT, { recursive: true });
  const wavs = (await readdir(SOUNDS)).filter((f) => f.endsWith(".wav"));
  for (const wav of wavs) {
    const id = wav.replace(/\.wav$/, "");
    // Requires audiowaveform: brew install audiowaveform
    const raw = execSync(
      `audiowaveform -i "${join(SOUNDS, wav)}" -o /tmp/${id}.json -b 8`,
      { encoding: "utf-8" },
    );
    void raw;
    const data = await readJson(`/tmp/${id}.json`);
    const peaks = data.data ?? data;
    await writeFile(join(OUT, `${id}.json`), JSON.stringify(peaks));
    console.error(`peaks: ${id}`);
  }
}

async function readJson(p: string) {
  const { readFile } = await import("node:fs/promises");
  return JSON.parse(await readFile(p, "utf-8"));
}

main();
```

**Fallback if audiowaveform unavailable:** generate 32-bar pseudo-random peaks from file size hash for v0 dev only — document in README.

- [ ] **Step 2: Run after wavs present**

```bash
pnpm generate:peaks
```

- [ ] **Step 3: Commit**

```bash
git add scripts/generate-peaks.ts apps/web/public/peaks
git commit -m "feat: add waveform peak generation for catalog cards"
```

---

### Task 7: packages/mcp — stdio server

**Files:**
- Create: `packages/mcp/package.json`, `packages/mcp/tsconfig.json`, `packages/mcp/src/index.ts`, `packages/mcp/src/catalog-loader.ts`

- [ ] **Step 1: Package setup**

`packages/mcp/package.json`:
```json
{
  "name": "woven-sfx-mcp",
  "version": "0.1.0",
  "type": "module",
  "bin": { "woven-sfx-mcp": "./dist/index.js" },
  "scripts": { "build": "tsc", "start": "node dist/index.js" },
  "dependencies": {
    "@modelcontextprotocol/server": "^1.0.0",
    "@woven-sfx/core": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": { "typescript": "^5.8.0" },
  "files": ["dist", "catalog.json"]
}
```

- [ ] **Step 2: Catalog loader**

`packages/mcp/src/catalog-loader.ts`:
```typescript
import { readFile } from "node:fs/promises";
import { join, dirname } from "node:path";
import { fileURLToPath } from "node:url";
import type { Catalog } from "@woven-sfx/core";

let cache: Catalog | null = null;

export async function loadCatalog(): Promise<Catalog> {
  if (cache) return cache;
  const dir = dirname(fileURLToPath(import.meta.url));
  const raw = await readFile(join(dir, "../catalog.json"), "utf-8");
  cache = JSON.parse(raw) as Catalog;
  return cache;
}
```

- [ ] **Step 3: MCP server**

`packages/mcp/src/index.ts`:
```typescript
#!/usr/bin/env node
import { McpServer } from "@modelcontextprotocol/server";
import { StdioServerTransport } from "@modelcontextprotocol/server/stdio";
import { z } from "zod";
import {
  searchCatalog,
  resolveForTransition,
  withLocalPath,
  pullSound,
  findSoundById,
  listInstalled,
} from "@woven-sfx/core";
import { loadCatalog } from "./catalog-loader.js";

const server = new McpServer({ name: "woven-sfx", version: "0.1.0" });

function textResult(obj: unknown) {
  return { content: [{ type: "text" as const, text: JSON.stringify(obj, null, 2) }] };
}

server.registerTool(
  "sfx_search",
  {
    title: "Search SFX catalog",
    description: "Search Woven SFX by tag, transition pairing, or query string",
    inputSchema: z.object({
      tag: z.string().optional(),
      transition: z.string().optional(),
      query: z.string().optional(),
    }),
  },
  async (input) => {
    const catalog = await loadCatalog();
    return textResult(searchCatalog(catalog, input));
  },
);

server.registerTool(
  "sfx_pull",
  {
    title: "Pull SFX to local library",
    description: "Download a sound by id to ~/.local/share/woven-sfx/library/",
    inputSchema: z.object({ id: z.string() }),
  },
  async ({ id }) => {
    const catalog = await loadCatalog();
    const sound = findSoundById(catalog, id);
    if (!sound) return textResult({ error: `Unknown sound: ${id}` });
    const localPath = await pullSound(sound);
    return textResult({ id, localPath, file: sound.file });
  },
);

server.registerTool(
  "sfx_resolve",
  {
    title: "Resolve SFX for transition",
    description: "Resolve and download the best SFX for a video transition type",
    inputSchema: z.object({
      transition: z.string(),
      moment: z.string().optional(),
    }),
  },
  async ({ transition }) => {
    const catalog = await loadCatalog();
    const resolved = resolveForTransition(catalog, transition);
    if (!resolved) {
      return textResult({
        error: `No default SFX for transition: ${transition}`,
        suggestions: searchCatalog(catalog, { transition }),
      });
    }
    const sound = findSoundById(catalog, resolved.id)!;
    const localPath = await pullSound(sound);
    return textResult(withLocalPath({ ...resolved, localPath }));
  },
);

server.registerTool(
  "sfx_list_installed",
  {
    title: "List installed SFX",
    description: "List .wav files in the local Woven SFX library",
    inputSchema: z.object({}),
  },
  async () => textResult({ files: await listInstalled() }),
);

async function main() {
  const transport = new StdioServerTransport();
  await server.connect(transport);
  console.error("woven-sfx MCP running on stdio");
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
```

- [ ] **Step 4: Build and smoke test**

```bash
cd packages/mcp && pnpm install && pnpm build
# Server should hang waiting for stdio — Ctrl+C to exit
node dist/index.js
```

- [ ] **Step 5: Commit**

```bash
git add packages/mcp
git commit -m "feat(mcp): add stdio MCP server with sfx tools"
```

---

### Task 8: Skill + install.sh

**Files:**
- Create: `packages/skill/SKILL.md`
- Create: `scripts/install.sh`
- Create: `apps/web/public/install.sh` (copy for static serve)

- [ ] **Step 1: Write SKILL.md**

`packages/skill/SKILL.md` — frontmatter:
```yaml
---
name: woven-sfx
description: Resolve and pull Woven sound effects for video edits. Use during /edit-plan (step 4 SFX placement) and before /assemble (ensure files cached). Requires woven-sfx MCP tools (sfx_resolve, sfx_pull, sfx_list_installed).
---
```

Body covers:
- Call `sfx_resolve({ transition })` per transition in edit plan
- Opening whoosh at 0.1s: `sfx_resolve({ transition: "whoosh" })`
- Use `suggested_volume` from response
- Before assemble: `sfx_list_installed` + `sfx_pull` for missing IDs
- Library path: `~/.local/share/woven-sfx/library/`

- [ ] **Step 2: Write install.sh**

`scripts/install.sh`:
```bash
#!/usr/bin/env bash
set -euo pipefail

SKILL_DIR="${HOME}/.claude/skills/woven-sfx"
LIB_DIR="${HOME}/.local/share/woven-sfx/library"
CDN="${SFX_CDN_BASE:-https://sfx.woven.video}"
REPO_RAW="${SFX_INSTALL_RAW:-https://sfx.woven.video}"

mkdir -p "$SKILL_DIR" "$LIB_DIR"

curl -fsSL "$REPO_RAW/skill.md" -o "$SKILL_DIR/SKILL.md"

echo "Installed skill → $SKILL_DIR"

# Download catalog and pull each sound
CATALOG=$(curl -fsSL "$CDN/catalog.json")
node -e "
const fs=require('fs');const path=require('path');const https=require('https');const http=require('http');
const catalog=JSON.parse(process.argv[1]);const lib=process.argv[2];
async function dl(url, dest) {
  if (fs.existsSync(dest)) return;
  await new Promise((res,rej)=>{(url.startsWith('https')?https:http).get(url,r=>{const d=[];r.on('data',c=>d.push(c));r.on('end',()=>{fs.writeFileSync(dest,Buffer.concat(d));res();});}).on('error',rej);});
}
(async()=>{for(const s of catalog.sounds){await dl(s.url, path.join(lib,s.file));console.log('  '+s.id);}})();
" "$CATALOG" "$LIB_DIR"

cat <<EOF

✓ Woven SFX installed

  Skill:   $SKILL_DIR
  Sounds:  $LIB_DIR

Add MCP to ~/.cursor/mcp.json (or Claude Desktop config):

{
  "mcpServers": {
    "woven-sfx": {
      "command": "npx",
      "args": ["-y", "woven-sfx-mcp"]
    }
  }
}

Catalog: $CDN/catalog.json
EOF
```

- [ ] **Step 3: Copy static assets**

```bash
cp packages/skill/SKILL.md apps/web/public/skill.md
cp scripts/install.sh apps/web/public/install.sh
chmod +x apps/web/public/install.sh
```

- [ ] **Step 4: Commit**

```bash
git add packages/skill scripts/install.sh apps/web/public/skill.md apps/web/public/install.sh
git commit -m "feat: add woven-sfx skill and install script"
```

---

### Task 9: apps/web — Astro scaffold + global styles

**Files:**
- Create: `apps/web/` via `pnpm create astro@latest`

- [ ] **Step 1: Scaffold Astro**

```bash
cd apps
pnpm create astro@latest web -- --template minimal --install --git false --typescript strict
cd web
pnpm add @astrojs/react @astrojs/tailwind react react-dom tailwindcss @tailwindcss/vite
pnpm astro add react --yes
```

- [ ] **Step 2: Configure astro.config.mjs**

```javascript
import { defineConfig } from "astro/config";
import react from "@astrojs/react";
import tailwindcss from "@tailwindcss/vite";

export default defineConfig({
  output: "static",
  integrations: [react()],
  vite: { plugins: [tailwindcss()] },
});
```

- [ ] **Step 3: Add stone palette globals**

`apps/web/src/styles/global.css`:
```css
@import "tailwindcss";

@theme {
  --color-stone-950: #0c0a09;
  --color-stone-900: #1c1917;
  --color-stone-800: #292524;
  --color-stone-600: #57534e;
  --color-stone-500: #78716c;
  --color-stone-400: #a8a29e;
  --color-stone-200: #e7e5e4;
  --font-sans: "Geist", system-ui, sans-serif;
}

html {
  background: var(--color-stone-950);
  color: #fafafa;
  font-family: var(--font-sans);
}
```

- [ ] **Step 4: wrangler.jsonc**

```jsonc
{
  "name": "woven-sfx",
  "compatibility_date": "2026-06-28",
  "assets": {
    "directory": "./dist",
    "not_found_handling": "404-page"
  }
}
```

- [ ] **Step 5: Verify dev server**

```bash
cd apps/web && pnpm dev
```

Expected: localhost serves blank page.

- [ ] **Step 6: Commit**

```bash
git add apps/web
git commit -m "feat(web): scaffold Astro site with tailwind"
```

---

### Task 10: apps/web — layout + hero split

**Files:**
- Create: `apps/web/src/components/SiteHeader.astro`, `SiteFooter.astro`
- Create: `apps/web/src/components/Hero.astro`, `InstallPanel.tsx`, `HowItWorksTimeline.astro`
- Modify: `apps/web/src/pages/index.astro`

- [ ] **Step 1: SiteHeader + SiteFooter**

Header: logo "Woven SFX", anchors `#catalog`, `#agents`, GitHub link `https://github.com/woven-video/woven-sfx`.

Footer: MIT + CC0, woven.video link, catalog version from `catalog.json`.

- [ ] **Step 2: InstallPanel.tsx (client component)**

Props: none. State: `expanded` boolean for manual config.

Renders:
- curl one-liner with copy button
- path hints (skill + library)
- collapsible section with MCP JSON + skill curl

Use `navigator.clipboard.writeText` on copy.

- [ ] **Step 3: HowItWorksTimeline.astro**

Vertical timeline per wireframe — 3 steps with dot + line CSS.

- [ ] **Step 4: Hero.astro**

Eyebrow: `MIT · CC0 sounds`
Headline: `Sound effects for AI agents`
Subhead: `One install. Your agent pulls whooshes, pops, and glitches into video edits.`
Grid: `InstallPanel` (client:load) | `HowItWorksTimeline`

- [ ] **Step 5: Wire index.astro**

```astro
---
import "../styles/global.css";
import SiteHeader from "../components/SiteHeader.astro";
import SiteFooter from "../components/SiteFooter.astro";
import Hero from "../components/Hero.astro";
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Woven SFX — Sound effects for AI agents</title>
    <meta name="description" content="Open source sound effects your agent can pull into video edits." />
  </head>
  <body>
    <SiteHeader />
    <main>
      <Hero />
    </main>
    <SiteFooter />
  </body>
</html>
```

- [ ] **Step 6: Visual check**

```bash
cd apps/web && pnpm dev
```

Verify split hero, expand works, mobile stacks (install then story).

- [ ] **Step 7: Commit**

```bash
git add apps/web/src
git commit -m "feat(web): add hero split with install panel and timeline"
```

---

### Task 11: apps/web — catalog with waveform cards

**Files:**
- Create: `apps/web/src/components/CatalogSection.tsx`
- Create: `apps/web/src/components/SfxCard.tsx`
- Create: `apps/web/src/components/SfxWaveform.tsx`
- Create: `apps/web/src/components/CopyButton.tsx`

- [ ] **Step 1: Install wavesurfer**

```bash
cd apps/web && pnpm add @wavesurfer/react wavesurfer.js
```

- [ ] **Step 2: SfxWaveform.tsx**

```tsx
import WavesurferPlayer from "@wavesurfer/react";

type Props = {
  url: string;
  peaksUrl?: string;
  playing: boolean;
  onFinish: () => void;
  wsRef: React.MutableRefObject<WaveSurfer | null>;
};

// Load peaks from peaksUrl via fetch in useEffect
// height={32}, waveColor="#57534e", progressColor="#fafafa"
// onReady: store ref; if playing, ws.play()
```

- [ ] **Step 3: SfxCard.tsx**

Layout per spec:
- Waveform bar top
- Row: 40px circular play button | name + duration + primary tag
- Copy id button

On play click: call `onPlay(id)` — parent stops others.

- [ ] **Step 4: CatalogSection.tsx**

- Fetch `/catalog.json` on mount (or import at build time)
- `useState` for `query` and `activeId`
- Filter sounds by id/tags
- Grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`
- Only one `activeId` plays at a time

- [ ] **Step 5: Add to index.astro**

```astro
import CatalogSection from "../components/CatalogSection.tsx";
// ...
<CatalogSection client:load />
```

- [ ] **Step 6: Manual QA**

- Search filters cards
- Play one sound, click another — first stops
- Copy id works
- Waveform renders (or graceful fallback if peaks 404)

- [ ] **Step 7: Commit**

```bash
git add apps/web/src/components
git commit -m "feat(web): add catalog grid with waveform preview cards"
```

---

### Task 12: Agent docs + llms.txt

**Files:**
- Create: `apps/web/src/components/AgentDocsStrip.astro`
- Create: `apps/web/public/llms.txt`

- [ ] **Step 1: llms.txt**

```
# Woven SFX
> Open source sound effects for AI agents.

## Install
curl -fsSL https://sfx.woven.video/install.sh | bash

## Catalog
https://sfx.woven.video/catalog.json

## MCP tools
sfx_search, sfx_pull, sfx_resolve, sfx_list_installed

## Skill
~/.claude/skills/woven-sfx/SKILL.md
```

- [ ] **Step 2: AgentDocsStrip + ToolList**

Section `id="agents"` with link cards + static tool descriptions.

- [ ] **Step 3: Commit**

```bash
git add apps/web
git commit -m "feat(web): add agent docs strip and llms.txt"
```

---

### Task 13: Cloudflare deploy

**Files:**
- Create: `apps/web/package.json` deploy script
- Document R2 upload in README

- [ ] **Step 1: Add deploy scripts**

`apps/web/package.json` scripts:
```json
{
  "build": "astro build",
  "deploy": "astro build && wrangler deploy",
  "preview": "astro build && wrangler dev"
}
```

`apps/web/devDependencies`: `wrangler`

- [ ] **Step 2: R2 upload (manual v0)**

```bash
# Install wrangler, authenticate
wrangler r2 object put woven-sfx/sfx/fast-whoosh.wav --file=sounds/fast-whoosh.wav
# Repeat per sound OR use r2 bulk script
```

Configure public access / custom domain `sfx.woven.video` in Cloudflare dashboard.

- [ ] **Step 3: Deploy site**

```bash
cd apps/web && pnpm deploy
```

- [ ] **Step 4: DNS**

CNAME `sfx.woven.video` → Workers subdomain from wrangler deploy output.

- [ ] **Step 5: Smoke test production**

- [ ] `curl -fsSL https://sfx.woven.video/install.sh | bash` (dry run on test machine)
- [ ] `https://sfx.woven.video/catalog.json` returns 27 sounds
- [ ] Catalog previews play audio
- [ ] MCP: `npx woven-sfx-mcp` + `sfx_resolve({ transition: "pull-in" })` in Cursor

- [ ] **Step 6: Commit**

```bash
git add apps/web wrangler.jsonc README.md
git commit -m "chore: add Cloudflare deploy config and R2 docs"
```

---

## Plan self-review

| Spec requirement | Task |
|---|---|
| Audio-forward landing | Task 9–11 |
| Split hero install + timeline | Task 10 |
| One-liner + expand | Task 10 InstallPanel |
| Waveform cards + big play | Task 11 |
| packages/core | Task 2–4 |
| MCP tools | Task 7 |
| Skill | Task 8 |
| install.sh | Task 8 |
| catalog.json | Task 5 |
| FOSS licenses | Task 1 |
| Cloudflare | Task 13 |
| No accounts | N/A |
| ~/.local/share/woven-sfx | Task 2 paths |

**Deferred (spec):** harness install, remote MCP Worker, sfx_sync_for_render — not in this plan.

**Open defaults resolved:**
- CDN: `https://sfx.woven.video`
- npm package: `woven-sfx-mcp` (unscoped)